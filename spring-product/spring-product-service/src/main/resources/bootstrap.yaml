spring:
  application:
    name: spring-product-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: gray
      config:
        server-addr: 127.0.0.1:8848
        shared-configs:
          - data-id: datasource.yaml
            group: DEFAULT_GROUP
            refresh: true
  config:
    import: nacos:spring-product-service.yaml?refreshEnabled=true&group=gray
server:
  port: 8021