spring:
  application:
    name: spring-gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
      config:
        server-addr: 127.0.0.1:8848
  config:
    import: nacos:spring-gateway-service.yaml?refreshEnabled=true&group=DEFAULT_GROUP
server:
  port: 8080